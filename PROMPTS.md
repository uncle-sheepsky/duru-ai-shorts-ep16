# STEP 0~10 — 이 영상을 만든 지시문

AI 에게 그대로 넘기면 되는 형태로 적었습니다. 자기 소재로 바꾸려면 STEP 1 부터 갈아 끼우세요.
각 STEP 은 **앞 STEP 이 확정한 값에 묶여 있습니다** — 순서를 바꾸면 뒤에서 터집니다.

> ⚠가장 자주 깨지는 곳: **길이를 바꾸면 전부 다시 해야 합니다.** 마디 수·프레임 수·나레
> 온셋이 전부 시프트합니다. 실제로 이 영상도 후킹 대사 한 줄이 늘어나면서 20.000초 →
> 22.000초로 다시 만들었습니다.

---

## STEP 0 — 격자부터 정한다 (길이·템포·컴프)

```
세로 쇼츠를 만들 거야. 25fps 고정 프레임레이트, 1080×1920.
내용은 이렇게 간다: [후킹 한 마디] → [결과물 3개를 1초씩] → [코드 화면] →
[결과물 2개를 2초씩] → [링크 안내] → [AI에 전달] → [자기참조] → [코드가 드러남] → [CTA].

먼저 격자부터 정해줘. 조건은:
· 컴프(화면 한 덩어리)가 대략 2초에 한 번 바뀔 것
· 결과물 구간이 정확히 1.000초 / 2.000초 단위로 떨어질 것
· 총 길이가 BGM 의 **정수 마디**로 떨어질 것 (안 그러면 음악 이음매가 생긴다)
· 컴프 경계가 전부 정수 프레임이면서 박 위에 앉을 것

가능한 BPM 과 총 길이 조합을 나열하고, 왜 그것만 되는지 계산을 보여줘.
값을 임의로 고르지 말고, 미정인 게 있으면 나한테 되물어.
```

→ 이 영상의 답: **BPM 120 · 22.000s · 550f · 11마디 · 44박**.
1.000초가 정수 박(2박)이 되는 템포가 120 계열뿐이고, 20.000초로는 후킹 대사가 안 들어갔습니다.
컴프 = `[4, 2,2,2, 4,4,4, 4,4,4, 4, 6]` 박.

---

## STEP 1 — 화면에 쓸 실물 프레임 뽑기

```
내가 전에 만든 영상 mp4 몇 개에서 프레임을 뽑아 쓸 거야.
· 소스는 **실제 게시본 mp4**로 해줘. 다시 렌더하면 시청자가 본 그 화면이 아니게 된다.
· 크롭 금지. 세로 영상이면 무크롭 풀블리드로 쓴다.
· 후보 시각을 5개쯤 뽑아서 한 장의 대조 시트로 만들어 줘. 내가 눈으로 고를게.
· 그리고 디코딩 메모리를 계산해 줘 — 1080×1920 PNG 를 여러 장 물면 브라우저가 죽는다.
  실제로 통과한 적 있는 규모 아래로 규격을 정해줘.
```

→ 이 영상의 답: **810×1440 을 12.5fps 로 25장**(컴프 프레임 `lf` 에 대해 `floor(lf/2)` 로 읽으면
25fps 실시간). 1080×1920 원본으로 물었으면 1GB 를 넘었습니다.

---

## STEP 2 — 나레이션

```
아래 대본으로 나레이션을 만들어 줘. (실녹음이면 녹음하고, TTS면 TTS로)
· 라인별 통합 라우드니스를 **같게** 맞춰줘. 라인 간 편차 0.3 LU 이하.
· 말단이 하드컷되지 않게 페이드 + 무음 패딩.
· 각 라인의 실제 길이를 초 단위로 알려줘. 그 값으로 배치를 계산할 거야.
```

---

## STEP 3 — 어절 타임스탬프 (여기가 이 파이프라인의 핵심)

```
나레이션 wav 들을 ASR 로 돌려서 **어절 단위 시작·끝 시각**을 뽑아 줘 (words.json).
표기가 대본과 다르면 **글자만** 대본으로 고치고 **시각은 절대 손대지 마.**
파일 해시를 남겨 줘 — 나중에 나레를 다시 만들면 이 파일이 낡았다는 걸 알아야 한다.
```

**왜 중요한가**: 컷 타이밍을 손으로 찍지 않습니다. *어절이 어디서 시작하는지*를 먼저 재고,
**그 시각에 컴프 경계가 오도록** 나레 시작 프레임을 역산합니다.

이 영상의 경우:
- N0 발화가 **정확히 2.000초**에 끝남 → 시연 시작(f50)과 오차 **0.0f**
- N1 「여러분도」 2.14초 → f175.5, 컴프 C5 시작 f175 과 **+0.5f**
- N2 「받아서」 1.40초 → f325.0, 컴프 C8 시작 f325 과 **0.0f**

```
나레 배치 프레임을 계산해 줘. 그리고 그 값을 빌드 스크립트에 **assert 로 박아** 줘.
1프레임 넘게 어긋나면 빌드가 실패해야 한다.
```

---

## STEP 4 — 배경음악

```
BGM 을 만들어 줘. 조건:
· BPM 과 총 길이는 STEP 0 에서 확정한 값
· 나레이션이 얹히니까 **300–3500Hz(사람 목소리 대역)를 비워** 줘
· 생성물은 샘플 0 이 마디 첫박이 아니다. 다운비트를 검출해서 **정수 마디로 잘라** 줘
· 후보를 3개 만들고, 후보마다 이걸 재서 표로 보여줘:
  보이스대 점유율 / 마디별 RMS / **컴프 경계마다 실제로 타격이 있는지**
· 단, 수치 1등을 자동 채택하지 마. 나는 귀로 고를 거야. 들을 수 있게 나레 얹은 믹스도 만들어 줘.
```

⚠**여기서 지표가 한 번 틀렸습니다.** "컴프 경계 타격이 최대인 창"으로만 고르게 했더니
트랙의 **페이드아웃 꼬리**를 집어서 마지막 2초가 무음이 됐습니다.
→ "어떤 마디도 창 중앙값보다 4dB 넘게 낮으면 그 창은 후보에서 제외"라는 하드 가드를 먼저 걸고,
그 안에서만 지표로 고르게 고쳤습니다. **지표가 내용을 정하게 두면 안 됩니다.**

---

## STEP 5 — 컴프 설계

```
컴프를 하나씩 그려 줘. 규칙:
· 화면 전체가 (컴프 번호, 그 컴프 안 프레임) 의 **순수함수**여야 한다.
  Math.random · Date.now · CSS transition/@keyframes · filter:blur 전부 금지.
· 첫 프레임부터 이미 뭔가 돌고 있어야 한다. 페이드인으로 시작하지 마.
· 한 화면에 **활성 주시점 하나**. 두 개를 동시에 읽게 만들지 마.
· 나레가 말하는 문장을 화면 타이포로 **다시 쓰지 마.** 문장은 자막 트랙이 맡고,
  컴프 안 글자는 라벨·파일명·수치·URL 만.
· 세로 쇼츠 세이프존: 의미를 지는 것은 x[108,886] · y[154,1536] 안에.
  화면 우측 아래는 좋아요·공유 버튼이 덮으니 CTA 를 거기 두지 마.
```

---

## STEP 6 — 화면에 코드를 띄운다면, 지어내지 못하게 만들어라

```
컴프 안에 내 소스 코드를 띄울 거야. 줄번호까지.
사람이 옮겨 적으면 반드시 틀리니까, **빌드 때 원문에서 뽑는 스크립트**를 만들어 줘.
· 발췌는 (파일, 시작 앵커 문자열, 줄 수)로만 지정한다
· 앵커가 원문에 정확히 1번 안 나오면 **빌드 실패**
· 발췌는 전부 연속 구간. 비연속을 붙여 연속인 척하지 않는다
· 원문 파일들의 sha256 을 산출물에 각인하고, 렌더러가 그걸 대조해서 안 맞으면 렌더를 거부
```

**이걸 안 했을 때 실제로 난 사고**: 앞 편에서 화면의 줄번호를 `032~036` 이라고 적었는데
실제로는 32·122·157·181·192(연속도 아님)였습니다. 각 화면은 「진짜」인데 **붙여 놓으니 거짓말**이 됐습니다.

---

## STEP 7 — 결정론 렌더

```
프레임을 굽는 렌더러를 만들어 줘.
· 시크해서 한 장씩 스크린샷
· 그리고 **같은 프레임들을 역순으로 다시 그려** 픽셀 해시가 같은지 확인
· 검사 지점 = 컴프 경계 전부 + 각 컴프 중앙 + 마지막 프레임 (경계가 제일 위험하다)
· 브라우저 래스터 플래그를 고정해 줘(서브픽셀·부분 래스터·GPU 래스터 끄기)
```

---

## STEP 8 — 오디오 믹스

```
BGM 과 나레를 섞어 줘.
· **덕킹 금지.** BGM 을 나레 때 자동으로 줄이지 마 — 먹먹해진다.
· 대신 발화 구간에서 재서 **에너지 지분 10:90(BGM:나레)** 이 되는 고정 게인 하나만 걸어라.
· 최종 −14 LUFS / 트루피크 −1 dBFS.
  loudnorm 으로 착지시키지 마 — 짧은 소재에서 측정값이 틀린다.
  **정적 게인 + 피크 삭감**으로 가고, 게인과 리미터 천장은 **따로 수렴**시켜라(같이 움직이면 발산한다).
```

---

## STEP 9 — 게이트 (자평 말고 측정)

```
게이트를 스크립트로 만들어 줘. 전부 실행·측정이어야 하고, 내가 "괜찮아 보인다"고 말하는 건 게이트가 아니다.
 G1 블랙/의미공백 프레임 0
 G2 컴프 전환 밀도 — 컴프 경계에서 실제로 픽셀이 크게 바뀌었는가
 G3 ffprobe 프레임 수 ↔ 렌더 장수 ↔ 화면에 쓴 상수 **3자 대조**
 G4 라우드니스 — 문서가 아니라 **mp4 파일을 다시 재라**
 G5 하단 플랫폼 UI 대역에 밝은 정보층이 없는가
 G6 컴프가 완성 상태로 몇 프레임 유지되는가
```

⚠**G2 와 G6 은 임계를 잘못 잡기 쉽습니다.**
- G6: 상시 모션(영상 재생·커서 점멸·자막)을 끄지 않고 "안 변하는 프레임 수"를 재면 전부 0 이 나옵니다.
  → **변해도 되는 것을 먼저 정의**하고 고정한 다음 재세요.
- G2: 전체 구간의 p90 을 기준으로 삼으면, 영상이 재생되는 앞부분 모션에 끌려 올라가서
  정적인 뒷부분이 오탐으로 걸립니다. → **인접 컴프 내부 분포**와 비교하세요.

그리고 실제로 G2 가 잡은 결함이 있었습니다: wipe 컴프가 앞 컴프의 마지막 상태로 시작해서
**경계 프레임 차이가 0.36 = 컷이 사라져** 있었습니다. 첫 프레임부터 이미 벗겨진 상태로 고쳤습니다.

---

## STEP 10 — 썸네일

```
첫 프레임(f0) 한 장만 썸네일로 따로 그려 줘. 40ms 라 재생에선 안 보인다.
후보를 3안 만들고 **200px 폭으로 축소한 대조 시트**를 같이 줘 —
피드에서 실제로 그 크기로 보이니까, 원본 크기로만 보면 판정이 안 된다.
그리고 하단 UI 대역(y>1700)에 밝은 게 내려가지 않았는지 게이트로 확인해 줘.
```

→ 이 영상의 답: 3안 중 **200px 에서 카피 2줄이 다 읽히고 타일이 컬러로 살아 있는 안**을 채택.
나머지 둘은 200px 에서 절반이 회색으로 뭉개지거나 전체가 검정에 가까웠습니다.
그리고 초판은 타일이 y1713 까지 내려가 G5 에 걸려서 규격을 줄였습니다.
